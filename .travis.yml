language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-costjabrify' ]; then cd .. && eval "mv $currentfolder generator-costjabrify" && cd generator-costjabrify; fi

